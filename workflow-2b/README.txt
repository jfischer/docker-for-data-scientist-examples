Run a shell environment that modifies local files, with user mapping. For the
direct linux version only. You need this if --userns-remap is set on the Docker
daemon (enabled by default in the enterprise edition). See
https://docs.docker.com/engine/security/userns-remap for details.
